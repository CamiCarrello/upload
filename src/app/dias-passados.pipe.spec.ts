import { DiasPassadosPipe } from './dias-passados.pipe';

describe('DiasPassadosPipe', () => {
  it('create an instance', () => {
    const pipe = new DiasPassadosPipe();
    expect(pipe).toBeTruthy();
  });
});
